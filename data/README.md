# Data folder

This folder is reserved for local CSV datasets or cached datasets.

For torchvision and MedMNIST datasets, the simulation downloads/caches data automatically under `./data` when run from the project directory.

Do not commit large raw datasets to Git. Keep only small metadata files or instructions here.
